# CHROMIST.ORG

Home to the Chromist.org site

# GETTING STARTED

* Initialize the submodule:
```
git submodule update --init
```

* Run with watch task:
```
jekyll serve --watch
```

* See the website running in:
```localhost:4000```